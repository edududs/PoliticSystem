commands:
  back:
    - cd core
    - uv run --active python manage.py runserver
  front:
    - cd ui
    - yarn dev
  test:
    - cd core
    - python local/testes/main.py
  shell:
    - cd core
    - uv run --active python manage.py shell_plus --ipython
